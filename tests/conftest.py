"""Pytest fixtures shared across the test suite.

Filled in as needed. Connector tests will load HTML fixtures from
`tests/fixtures/` here so each connector's parser can be exercised
fully offline.
"""

from __future__ import annotations
