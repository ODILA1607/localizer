"""Connectors per source.

Adding a new source = drop in one module that implements the `Connector`
protocol from `base.py` plus one fixture under `tests/fixtures/`. Nothing
else in the codebase needs to change.
"""
